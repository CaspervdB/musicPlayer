<Window x:Class="MediaPlayer.DeletePlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayer"
        mc:Ignorable="d"
        Title="DeletePlaylist" Height="328.83" Width="478.105">
    <Window.DataContext>
        <local:DeletePlaylistViewModel/>
    </Window.DataContext>
    <Grid>
        <ComboBox DisplayMemberPath="PlaylistName" Name="ComboBox" ItemsSource="{Binding Playlists}" SelectedItem="{Binding SelectedPlaylistInDeleteWindow}" HorizontalAlignment="Left" Margin="193,73,0,0" VerticalAlignment="Top" Width="211"/>

        <Button Content="Delete Playlist" HorizontalAlignment="Left" VerticalAlignment="Top" Width="363" Margin="41,186,0,0" Height="45" Command="{Binding DeletePlaylist}"/>
        <Label Content="Select a playlist to delete: " HorizontalAlignment="Left" Margin="41,69,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
